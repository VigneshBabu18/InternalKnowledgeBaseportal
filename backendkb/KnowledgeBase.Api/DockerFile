# Use Node.js base image
FROM node:18

# Set working directory
WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy the rest of the source code
COPY . .

# Expose the dev server port (usually 3000)
EXPOSE 3000

# Start the app in development mode
CMD ["npm", "start"]
